module.exports = {
    // const path = require('path')

    entry: '../main.js',
    module:{
        rules: {
            // { test: /\/css$/, use: ['style-loader', 'css-loader']},
            // { test: /\.(js)$/, use: 'babel-loader' }
        }
    }
}